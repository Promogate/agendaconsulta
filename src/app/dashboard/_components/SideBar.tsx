export function SideBar() {
  return (
    <aside
      className={`w-64 bg-white lg:bg-gray-50 border-r border-gray-200 lg:block lg:relative absolute inset-y-0 left-0 z-40 transform transition-transform duration-300 ease-in-out lg:translate-x-0`}
    >
      <p>Sidebar</p>
    </aside>
  );
}